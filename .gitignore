# ─── HFO Gen90 .gitignore ───────────────────────────────────
# Rule: root stays clean. Forge holds everything.
# Rule: secrets never committed. Artifacts are ephemeral.

# ── Secrets ──────────────────────────────────────────────────
.env
.hfo_secret
*.pem
*.key

# ── Python ───────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/

# ── Node (if used) ───────────────────────────────────────────
node_modules/
package-lock.json

# ── IDE ──────────────────────────────────────────────────────
.vscode/settings.json
.vscode/mcp.json
.idea/
*.swp
*.swo
*~

# ── OS ───────────────────────────────────────────────────────
Thumbs.db
.DS_Store
desktop.ini

# ── Root scratch scripts (operator one-offs, never commit) ───
/_*.py
/_*.ps1
/_*.txt

# ── Root state/output files ─────────────────────────────────
/*.txt
/*.sqlite
.hfo_*.json
.p7_*.json
.hfo_models/

# ── Daemon runtime state files ──────────────────────────────
.prey8_session_state.json
.prey8_session_*.json
.watchdog_watermark.json
--reap

# ── Ephemeral artifacts (never commit temp outputs) ──────────
artifacts/tmp/
*.log
temp/
fleet_monitor_run*.log

# ── Generated concat/compilation artifacts (too large for git) ──
*_CONCAT_*.md
*_MEGA_COMPILATION*.md

# ── Omega v13/v15 build artifacts ───────────────────────────
test-results/
reports/
*.tsbuildinfo

# ── Stryker mutation testing temp/output ─────────────────────
.stryker-tmp/
**/stryker_*.txt

# ── Test coverage artifacts ───────────────────────────────────
.coverage
htmlcov/
.pytest_cache/
coverage.xml

# ── Project-level temp output files ──────────────────────────
**/jest_hud_out.txt
**/test_output.txt
**/tsc_output.txt
**/todos.txt

# ── SQLite WAL/SHM (DB itself IS committed via LFS) ─────────
*.sqlite-wal
*.sqlite-shm
*.sqlite-journal

# Session scratch scripts (housekeeping)
_scratch/

# ── Root one-off operator scripts (not forge, not source) ────
/check_ssot.py
/cosmic-ray.toml
/manual_prey8.py
/doc12.md

# ── VSCode extension packages (generated artifacts) ──────────
*.vsix
commit_output.txt
